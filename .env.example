# Example environment variables for GYM-back
# Copy this file to `.env` and fill values before running locally.

# Server port
PORT=3000

# Database connection string (example for MongoDB)
# MONGODB_URI=mongodb://localhost:27017/gymdb

# JWT secret for signing tokens
JWT_SECRET=your_jwt_secret_here

# Any other secrets or API keys
# STRIPE_KEY=sk_test_...
# SENDGRID_API_KEY=SG.xxxxx
