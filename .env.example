# Gym Management System - Environment Variables
# Copy this file to `.env` and fill in your actual values

# Server Configuration
PORT=5000
NODE_ENV=development
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Frontend URL (for email verification links and CORS)
FRONTEND_URL=http://localhost:3000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gym_db
DB_USER=postgres
DB_PASSWORD=your-database-password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRES_IN=7d

# Email Configuration (Optional - leave empty to skip email verification)
EMAIL_SERVICE=gmail
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Email Setup Instructions:
# 1. For Gmail, enable 2FA in your Google Account
# 2. Go to Google Account > Security > 2-Step Verification > App passwords
# 3. Generate an app password and use that as EMAIL_PASSWORD
# 4. Do NOT use your regular Gmail password

# Google OAuth Configuration (Optional)
GOOGLE_CLIENT_ID=your-google-client-id-from-console
GOOGLE_CLIENT_SECRET=your-google-client-secret-from-console
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# OAuth Setup Instructions:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add authorized redirect URIs

# App Configuration
APP_NAME=Gym Management System
